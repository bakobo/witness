# Prove the LMDB upgrade path across keripy pins (OPS-F5). Bringing a new image up on an older image's volume without re-incepting is undocumented and untested, and whether kli migrate run is ever needed between pins is unknown. It is the operation that will matter most the first time it is needed and the worst one to discover live. Wants a test that starts image A, writes state, then starts image B on the same volume and asserts the AID, KEL and receipts survive and a new event is still accepted.
kind: todo
created: 2026-09-04T02:05Z

- 2026-09-04T15:22Z Done 2026-09-04. tests/test_image_upgrade.py drives the real sequence against real containers: start, get a controller's signed inception accepted over the witness's HTTP port, REPLACE the container (not restart — a new digest is a replacement), assert AID + witnessed key state survive and a new event is still accepted. Found two defects: (1) keripy validates the DB version after lmdb.open succeeds, so a Baser we never closed leaked into py-lmdb's per-process table and every request after the first reported 'already open in this process' instead of the real cause; (2) a DB awaiting migration was reported as retryable DbUnavailable, so an alert would back off forever for something only kli migrate run clears. Now MigrationRequired / DatabaseTooNew, classified by keripy exception TYPE with contract tests. Runbook in docs/deploying.md, including the one-way-door finding: an upgrade across a migration cannot be rolled back by redeploying the previous digest, so back up first. NOT proven: no upgrade across an actual migration has been rehearsed, because the pin has not moved — the mechanism is tested, the migration itself has been read.
