# Registrar-to-Observer push is a demo kludge: a signed batch every window, empty when nothing changed (Q-ZGF7, SEDI summit). Replace the transport with a webhook, SSE or better. Keep a liveness signal, a monotonic batch number and a window end time, so the Observer can still tell 'quiet' from 'cut off' and measure freshness
kind: debt
tags: registrar, demo-kludge
created: 2026-09-24T21:38Z

