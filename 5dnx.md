# A witness started over a volume with NO KEYSTORE silently creates one, and every black-box signal says it is healthy. Measured 2026-09-05 while building infra's tier-one restore drill: delete ks/ from a volume, start the container, and it reports {"status": "ok"}, serves the ORIGINAL AID from the database's hab record, accepts a controller inception over HTTP with a 204, and `witness backup` succeeds. That is @7kwmhg's disaster wearing a green light -- the same AID every validator still trusts, against a signing key that is not the one that AID names. A restore from a keystore-less backup produces exactly this and nothing tells the operator. Suggested: refuse to start when the keystore is absent but the database holds a hab, since that combination cannot be legitimate; at minimum surface it in /v1/witness/health, which currently cannot distinguish it from healthy. infra's drill now asserts on the restored TREE before starting anything, because it could not be inferred from the running container.
kind: todo
created: 2026-09-05T22:15Z

- 2026-09-06T02:14Z A second consumer now depends on how this is fixed. bakobo/infra's observability session is adding an OTel Collector per witness (ops.md §7) and has widened its scope on the strength of this finding: it wants a signal distinguishing a witness signing with the key its AID NAMES from one that is not -- the AID's expected signing key against what the keystore actually holds -- exposed on the control plane so the collector can gauge it. They have deliberately NOT invented that surface, on the grounds that if this tick's fix changes what the control plane exposes, the collector should consume the new thing rather than a metric defined now and retired later. So whoever fixes this should decide the surface as well as the refusal: a start-time refusal alone leaves the running-witness case unobservable, and /v1/witness/health returning a third state, or an explicit key-matches-AID field, is what the collector would read. Coordinated 2026-09-06 via /tmp/infra-coord/.
