# Three fixes belong in the CANONICAL copilot-review-gate.yml in bakobo/infra (and bakobo.com), not in witness — this repo vendors the file verbatim and divergence is the problem the vendoring discipline exists to avoid. Raised by Copilot on witness#7, 2026-09-11, all three judged real. (1) Opposing concurrent runs are not serialized: a stale run for a ready PR can POST after a newer [no-ccr] run's DELETE, leaving Copilot assigned against the current decision; per-PR concurrency with cancel-in-progress is the cheap shape since the gate is idempotent. (2) e.review.not_requested.f is permanent for every case including a POST that failed on a rate limit or a network error, where a rerun may well succeed; split .r for a call that errored from .f for a call that returned success and assigned nobody. (3) In the REMOVE step, a failed timeline read is indistinguishable from 'not requested' because only the function's output is compared and not its exit status, so a transient API failure reports a successful removal and leaves Copilot assigned — fail-open in the branch whose whole job is to honour [no-ccr]. Declined a FOURTH comment on evidence: Copilot claimed the timeline endpoint needs issues:read, but witness#7's own gate run (34637690407) did the timeline read under pull-requests:write alone and printed 'Copilot requested.'
kind: todo
created: 2026-09-11T19:28Z

