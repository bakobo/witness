# Bump the keripy pin once WebOfTrust/keripy accepts (or renames) the /decl reply routes. The control plane's signed-declaration read path is written and 100%-covered but dormant: reader._declared() reads db.decls through getattr, and the pinned keripy (366d8107) has no such store, so /v1/witness/tags and /v1/witness/attribs always report source=operator-config against a real witness. tests/test_keripy_contract.py::test_the_pinned_keripy_has_no_decl_stores_yet asserts that absence and will FAIL when the pin moves -- that failure is the signal, delete the test then. Two things to check at bump time: (1) upstream may rename the routes (the PR explicitly invites a spelling other than /decl), which changes the kind keys 'tags'/'attribs' passed to _declared() and the store names db.decls/db.dans; (2) @w7c4mz rejects diverging code, so do NOT pin to the bakobo branch to get this early -- wait for the upstream commit. PR: bakobo/keripy feature/witness-decls -> WebOfTrust/keripy.
kind: todo
created: 2026-09-16T20:15Z

