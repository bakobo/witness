# tests/test_image_upgrade.py's _volume_for() leaks a volume when kli init fails. docker volume create happens BEFORE the try, so a failure in the initial 'docker run ... kli init' -- an unpullable image, a stale ghcr credential, a broken keystore -- exits without reaching the finally that sweeps by prefix. Found by a codex review on PR #17, 2026-09-21, which correctly noted the commit under review neither introduced nor worsened it; ~4ytc closed the sibling leak and missed this path. Fix: move the create+init inside the try, or register the volume for sweeping before creating it. Same file also calls _same_pin_or_skip() in the test bodies, so a pin mismatch builds a baseline volume and runs kli init before skipping -- move it into the fixture beside _two_images_or_fail() while you are there.
kind: todo
created: 2026-09-21T22:33Z

- 2026-09-26T08:53Z PR bakobo/witness#20 (2026-09-26) fixes this; tick off when it merges.
