# Pool witnesses cannot be tagged without a second tag source. `witness pool up` should mark its witnesses testnet + bakobo.pool (a pool IS test infrastructure, and today its AIDs are indistinguishable from production ones), but the obvious mechanism collides with an existing decision: pool.py deliberately does NOT override the image CMD (module docstring, ~line 19), because restating the image's command in the pool code would drift silently the first time the image's own CMD changed. So --tag cannot reach the pooled control plane on argv. Candidates: (a) a WITNESS_TAGS env var passed with docker -e, which leaves CMD alone but is a NEW input door needing its own bounding per dev/standards/input-handling.md; (b) a tags file seeded into the volume the way the advertised URL already is; (c) revisit the CMD-override decision. Each needs an intent node -- @nlunqygr freezes --tag as the only source today. Not scope creep to be done quietly; pick one deliberately.
kind: todo
created: 2026-09-16T18:32Z

- 2026-09-16T18:49Z Widened by @e4ceoopg: a pooled witness now wants BOTH a bakobo.pool tag (the predicate 'this is pool infrastructure') and a pool=<name> attribute (which pool). Same delivery obstacle for both -- neither can reach the pooled control plane on argv without overriding the image CMD -- so whichever mechanism is chosen must carry tags and attributes alike. That argues mildly for the volume-seeded file over a WITNESS_TAGS env var, since one file can carry both without inventing two env vars.
