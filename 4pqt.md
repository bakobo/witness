# A witness that has never witnessed anything reports TWO controllers from /v1/witness/controller, not one. Measured 2026-09-11 on a fresh volume, in a pooled container and in a plain one alike: db.states holds two icp key states — the witness's own AID (the one /v1/witness/identity returns) and a second non-transferable AID that is NOT in hby.habs, so it is something keripy's own witness setup incepts rather than a controller anybody designated. The endpoint presents both as 'controllers', which is misleading for the endpoint whose whole job is 'whom does this witness witness'. Find out what the second AID is (a mailbox/agent hab under a namespace is the guess, unverified), then decide: exclude it, or label the kind. docs/pools.md tells readers to read the count as a delta, which is a workaround, not a fix.
kind: todo
created: 2026-09-11T18:26Z

- 2026-09-26T09:02Z Identified 2026-09-26 (probe against an image built from bd76618): the second AID is keripy's Habery Signator -- habbing.Signator, keri/app/habbing.py:874, alias SIGNER='__signatory__' (:871). It is a hidden non-transferable AID that every Habery incepts on first open to sign and verify data at rest, so it is not in hby.habs and nobody designated it. Also note the FIRST entry is the witness's own AID, so a fresh witness reports zero real controllers, not one. The read-only reader can tell both apart without opening a Habery: db.hbys.get('__signatory__') gives the signator's prefix and db.habs holds the witness's own. Options for Daniel: (a) exclude both from /v1/witness/controller, since neither is a controller the witness witnesses; (b) keep them but add a kind field (self / signator / controller); (c) exclude the signator only and keep self, which is witnessed by itself in a trivial sense. (a) matches the endpoint's purpose, 'whom does this witness witness'; (b) keeps the full key-state inventory visible. After either, the delta-reading workaround in docs/pools.md can go. Probe script: /tmp/4pqt-probe.sh.
- 2026-09-26T09:21Z Context from tests/test_reader.py::test_controllers_includes_the_witnesss_own_key_state: listing the witness's own AID was a deliberate choice ('an operator asking what state exists wants all of it'). So option (b), labelling kinds, is closer to that intent than (a); the signator is the part nobody chose.
