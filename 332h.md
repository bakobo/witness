# test_the_running_witness_publishes_telemetry is flaky and it gates image publication. It failed the v0.2.0 tag build (run 35804917491, 2026-09-23) while the identical tree passed on the main push and the PR run, then passed on re-run with no change. The failure is the FIRST snapshot -- 'docker exec ... SegmentReader(telemetry).read()' exiting 1 -- after _await_healthz has already waited for a tick count, so it is not the startup race that helper's docstring describes. Most likely a torn read of a segment the TelemetryDoer is writing concurrently. Fix the diagnosis first: tests/test_image_smoke.py's _docker helper captures stderr, but pytest shows only the CalledProcessError repr, so the exception the container actually raised is nowhere in the CI log -- a release-gating failure that does not say why. Then decide whether SegmentReader should tolerate a partial trailing record.
kind: todo
created: 2026-09-23T02:35Z

- 2026-09-26T08:58Z PR bakobo/witness#21 (2026-09-26): cause was reader attempt exhaustion (1 in ~1,800 reads), not a torn record; tick off when it merges.
