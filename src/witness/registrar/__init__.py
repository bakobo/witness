"""An issuer's Registrar, co-hosted with its witnesses (@r3aonvlz).

The issuer publishes each settled TEL head here; Observers subscribe once and receive one signed
batch per window (@ed3dkgl5, @3m2eys6w). Nothing here verifies a chain cryptographically — the
Observer does, under the verifier's governance (@5m2m4ozz) — and nothing here can be pulled.
The wire protocol is ours; no specification defines one yet.
"""
