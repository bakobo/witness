# When bumping the heti pin past PR #2, swap heti_errors.HetiError for BakoboError in test_heti_contract.py
kind: todo
created: 2026-08-06T21:20Z

- 2026-08-06T23:31Z Also swap the two code strings in test_heti_contract.py that assert against heti's registry: e.feature.unsupported.trans-aid.f becomes e.feature.unsupported.aid.trans.f, and e.proof.request-sig.f becomes e.proof.request.sig.f. Both are contract assertions against the PINNED heti, so they must move with the pin, not before it.
- 2026-08-08T01:48Z Third item for the pin bump: test_heti_contract.py:89-91 call heti_errors.matches(), which the shared package has retired in favour of is_a/is_exactly/is_like. Line 91 also asserts e.*.f matches e.input.range.f — true under fnmatch, FALSE under the new segment semantics, where * is exactly one segment. Use is_a("e.input") or is_like("e.**.f").
